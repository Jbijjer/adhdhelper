{
  "name": "adhdhelper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"node server/index.js\" \"client/node_modules/.bin/vite --config client/vite.config.js\"",
    "build": "client/node_modules/.bin/vite build --config client/vite.config.js",
    "start": "NODE_ENV=production node server/index.js",
    "generate-vapid": "web-push generate-vapid-keys"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
